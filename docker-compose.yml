services:
  mariadb:
    image: mariadb:10.11
    container_name: ${MARIADB_NOMBRE_CONTENEDOR}
    environment:
      - MARIADB_USER=${MARIADB_USER}
      - MARIADB_PASSWORD=${MARIADB_PASSWORD}
      - MARIADB_DATABASE=${MARIADB_DATABASE}
      - MARIADB_ROOT_PASSWORD=${MARIADB_ROOT_PASSWORD}
    volumes:
      - mariadb_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MARIADB_ROOT_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 5

  moodle:
    build:
      context: ./src/moodle
      dockerfile: Dockerfile
    container_name: ${MOODLE_NOMBRE_CONTENEDOR}
    ports:
      - "${MOODLE_PUERTO_HOST}:${MOODLE_PUERTO_CONTENEDOR}"
    environment:
      - BITNAMI_DEBUG=true
      - MOODLE_REVERSEPROXY=no
      - MOODLE_DATABASE_HOST=mariadb
      - MOODLE_DATABASE_PORT_NUMBER=3306
      - MOODLE_DATABASE_USER=${MARIADB_USER}
      - MOODLE_DATABASE_PASSWORD=${MARIADB_PASSWORD}
      - MOODLE_DATABASE_NAME=${MARIADB_DATABASE}
      - MOODLE_USERNAME=${MOODLE_USERNAME}
      - MOODLE_PASSWORD=${MOODLE_PASSWORD}
      - MOODLE_EMAIL=${MOODLE_EMAIL}
      - MOODLE_SITE_NAME=LLM Wiki Assistant Moodle
      - MOODLE_HOST=${DOMAIN}:${MOODLE_PUERTO_HOST}
      - MAPEO_API_TOKEN=${MAPEO_API_TOKEN}
      - MATRIX_ACCESS_TOKEN=${MATRIX_ACCESS_TOKEN}
      - SYNAPSE_URL_INTERNA=http://synapse:8008
    depends_on:
      mariadb:
        condition: service_healthy
    volumes:
      - moodle_data:/bitnami/moodle
      - moodledata_data:/bitnami/moodledata
      - ./src/moodle/block_bdc:/moodle-custom-plugin
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'echo > /dev/tcp/127.0.0.1/8080'"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 180s
    networks:
      - default
      - bdc-net

  synapse:
    image: matrixdotorg/synapse:latest
    container_name: ${SYNAPSE_NOMBRE_CONTENEDOR}
    ports:
      - "${SYNAPSE_PUERTO_HOST}:${SYNAPSE_PUERTO_CONTENEDOR}"
    environment:
      - SYNAPSE_SERVER_NAME=${DOMAIN}
      - SYNAPSE_REPORT_STATS=no
      - PYTHONPATH=/data
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
      - SYNAPSE_CONFIG_ENABLE_ENV=yes
      - SYNAPSE_REGISTRATION_SECRET=${SYNAPSE_REGISTRATION_SECRET}
      - SYNAPSE_MACAROON_SECRET=${SYNAPSE_MACAROON_SECRET}
      - SYNAPSE_FORM_SECRET=${SYNAPSE_FORM_SECRET}
    volumes:
      - ./src/matrix/synapse-data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8008/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  element:
    image: vectorim/element-web:latest
    container_name: ${ELEMENT_NOMBRE_CONTENEDOR}
    ports:
      - "${ELEMENT_PUERTO_HOST}:${ELEMENT_PUERTO_CONTENEDOR}"
    volumes:
      - ./src/matrix/element/config.json:/app/config.json:ro
      - ./src/matrix/element/default.conf.template:/etc/nginx/templates/default.conf.template:ro
    depends_on:
      synapse:
        condition: service_healthy

  mapeo-api:
    build:
      context: ./src/api
    image: moodle-matrix-dev-mapeo-api
    container_name: ${MAPEO_API_NOMBRE_CONTENEDOR}
    environment:
      - MAPEO_API_TOKEN=${MAPEO_API_TOKEN}
      - GITHUB_PAT=${GITHUB_PAT}
      - DATABASE_URL=${DATABASE_URL}
      - PYTHONPATH=/shared-pkg
    volumes:
      - mapeo_api_data:/data
      - ./config:/config:ro
      - ./src/shared:/shared-pkg:ro
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - default
      - bdc-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:${MAPEO_API_PUERTO_CONTENEDOR}/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  postgres:
    image: postgres:16-alpine
    container_name: moodle-matrix-dev-postgres-1
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    networks:
      - default
      - bdc-net
    volumes:
      - postgres_api_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  maubot:
    build:
      context: ./src/bot
      dockerfile: Dockerfile
    container_name: ${MAUBOT_NOMBRE_CONTENEDOR}
    environment:
      - MAPEO_API_TOKEN=${MAPEO_API_TOKEN}
      - GITHUB_PAT=${GITHUB_PAT}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - PYTHONPATH=/shared-pkg
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
    command: ["sh", "-c", "python -c \"import os, yaml; c=yaml.safe_load(open('/data/config.yaml')); c['database']=os.environ['DATABASE_URL']; c['crypto_database']=os.environ['DATABASE_URL']; yaml.dump(c, open('/tmp/config.yaml','w'))\" && exec python -m maubot -c /tmp/config.yaml"]
    ports:
      - "${MAUBOT_PUERTO_HOST}:${MAUBOT_PUERTO_CONTENEDOR}"
    volumes:
      - ./src/bot:/data
      - ./config:/config:ro
      - ./src/shared:/shared-pkg:ro
    depends_on:
      synapse:
        condition: service_healthy
      pgvector:
        condition: service_healthy
    networks:
      - default
      - bdc-net

  pgvector:
    image: pgvector/pgvector:pg16
    container_name: moodle-matrix-dev-pgvector-1
    environment:
      POSTGRES_USER: llm_wiki
      POSTGRES_PASSWORD: llm_wiki_pass
      POSTGRES_DB: vector_store
    ports:
      - "5432:5432"
    volumes:
      - pgvector_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U llm_wiki -d vector_store"]
      interval: 10s
      timeout: 5s
      retries: 5

  ollama:
    image: ollama/ollama:latest
    container_name: ${LLM_SERVER_OPCIONAL_NOMBRE_CONTENEDOR}
    profiles:
      - ollama
    ports:
      - "${LLM_SERVER_OPCIONAL_PUERTO_HOST}:${LLM_SERVER_OPCIONAL_PUERTO_CONTENEDOR}"
    volumes:
      - ollama_data:/root/.ollama

  redis:
    image: redis:7
    container_name: moodle-matrix-dev-redis-1
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "6379:6379"
    networks:
      - default
      - bdc-net
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  sync-worker-1:
    build:
      context: ./src/bot
      dockerfile: Dockerfile
    container_name: moodle-matrix-dev-sync-worker-1
    working_dir: /data/llm-wiki-assistant-plugin
    command: ["rq", "worker", "sync-jobs", "log-jobs", "--url", "redis://redis:6379"]
    environment:
      - MAPEO_API_TOKEN=${MAPEO_API_TOKEN}
      - GITHUB_PAT=${GITHUB_PAT}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - INTERNAL_SERVICE_TOKEN=${INTERNAL_SERVICE_TOKEN}
      - MAPEO_API_URL=http://moodle-matrix-dev-mapeo-api-1:8000
      - METRICS_API_URL=http://metrics-api:8000
      - PYTHONPATH=/data/llm-wiki-assistant-plugin:/shared-pkg
    volumes:
      - ./src/bot:/data
      - ./config:/config:ro
      - ./src/shared:/shared-pkg:ro
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - default
      - bdc-net

  sync-worker-2:
    build:
      context: ./src/bot
      dockerfile: Dockerfile
    container_name: moodle-matrix-dev-sync-worker-2
    working_dir: /data/llm-wiki-assistant-plugin
    command: ["rq", "worker", "sync-jobs", "log-jobs", "--url", "redis://redis:6379"]
    environment:
      - MAPEO_API_TOKEN=${MAPEO_API_TOKEN}
      - GITHUB_PAT=${GITHUB_PAT}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - INTERNAL_SERVICE_TOKEN=${INTERNAL_SERVICE_TOKEN}
      - MAPEO_API_URL=http://moodle-matrix-dev-mapeo-api-1:8000
      - METRICS_API_URL=http://metrics-api:8000
      - PYTHONPATH=/data/llm-wiki-assistant-plugin:/shared-pkg
    volumes:
      - ./src/bot:/data
      - ./config:/config:ro
      - ./src/shared:/shared-pkg:ro
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - default
      - bdc-net

  backup:
    build:
      context: ./src/backup
    container_name: moodle-matrix-dev-backup-1
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    volumes:
      - ./scripts:/scripts:ro
      - ./backups:/backups
      - redis_data:/redis_data:ro
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "ps", "aux", "|", "grep", "[c]rond"]
      interval: 30s
      timeout: 10s
      retries: 3

  metrics-api:
    image: moodle-matrix-dev-mapeo-api
    pull_policy: never
    depends_on:
      mapeo-api:
        condition: service_healthy
    container_name: metrics-api
    command: ["sh", "-c", "pip install -e . && alembic upgrade head && uvicorn metrics_api.main:app --host 0.0.0.0 --port 8000"]
    volumes:
      - ./src/metrics-api:/app
      - ./config:/config
      - ./src/shared:/shared-pkg:ro
    working_dir: /app
    environment:
      - PYTHONPATH=/shared-pkg
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      - JWT_SECRET_KEY=dev_secret_key_for_local_testing_only
      - MAPEO_API_TOKEN=${MAPEO_API_TOKEN}
      - MOODLE_AUTH_URL=http://moodle:8080/login/token.php
      - MAPEO_API_URL=http://mapeo-api:8000
      - ENABLE_DEMO_AUTH=true
      - ENVIRONMENT=dev
      - INTERNAL_SERVICE_TOKEN=${INTERNAL_SERVICE_TOKEN}
      - PII_SECRET_KEY=${PII_SECRET_KEY}
      - GITHUB_TOKEN=${GITHUB_PAT}
      - GITHUB_TOKEN_AGENT=${GITHUB_PAT}
      - ENABLE_EVALUATION_AGENT=${ENABLE_EVALUATION_AGENT}
      - GEMINI_API_KEY=${GEMINI_API_KEY}

  metrics-worker:
    image: moodle-matrix-dev-mapeo-api
    pull_policy: never
    depends_on:
      mapeo-api:
        condition: service_healthy
    container_name: metrics-worker
    command: ["sh", "-c", "export PYTHONPATH=/shared-pkg && pip install -e /metrics-api && pip install -e . && python -m metrics_worker.worker"]
    volumes:
      - ./src/metrics-worker:/app
      - ./src/metrics-api:/metrics-api
      - ./src/shared:/shared-pkg
    working_dir: /app
    environment:
      - MOCK_SERVICES=false
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      - GITHUB_TOKEN=${GITHUB_PAT}
      - MAPEO_API_URL=http://mapeo-api:8000
      - MAPEO_API_TOKEN=${MAPEO_API_TOKEN}

  frontend:
    image: node:20-alpine
    container_name: metrics-frontend
    command: ["sh", "-c", "npm install && npm run dev"]
    volumes:
      - ./src/frontend:/app
    working_dir: /app
    ports:
      - "3001:3000"

volumes:
  mariadb_data:
  moodle_data:
  moodledata_data:
  ollama_data:
  mapeo_api_data:
  postgres_api_data:
  pgvector_data:
  redis_data:

networks:
  bdc-net:
